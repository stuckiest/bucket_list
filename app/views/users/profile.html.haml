.container
  =link_to 'New', new_user_goal_path(@user), class: 'btn-floating btn-large waves-effect waves-light blue darken-3 right'
  %h1.center-align.white-text Welcome #{@user.first_name}

  - @goals.each do |goal|
    %h4
      = goal.title
    %p
      = goal.description
    %p
      = goal.location
    %p
      Completed:
      = goal.completed
    %p
      - goal.category.each do |goaleach|
        = goaleach
        |
    %p
      =link_to 'Edit', edit_user_goal_path(@user, goal), class: 'btn-floating btn-large waves-effect waves-light blue darken-3'
      =link_to 'Delete', profile_path(current_user.id), method: :delete, class: 'btn-floating btn-large waves-effect waves-light blue darken-3'
      -# 'btn-floating.btn-large.waves-effect.waves-light.blue.darken-3'
      -# %i.material-icons add

      -# <%= link_to 'Destroy', owner_path(owner), method: :delete, data: { confirm: 'Are you sure?' } %>
